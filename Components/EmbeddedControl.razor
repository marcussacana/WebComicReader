@if (BookLoader.EmbeddedFunctions)
{
    <div class="fixed-bottom">
        <a href="#" class="btn" onclick="embedded.close(); return false;">Close</a>
        <a href="#" class="btn" onclick="embedded.next(); return false;">Next</a>
    </div>
}